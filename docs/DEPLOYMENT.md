# Deployment

ABG Pulse v5 deployment source of truth: `main` branch of `NinjaRK/abg-pulse`.

This file was added to trigger and verify the GitHub → Vercel deployment pipeline after connecting the repository to the existing Vercel project.
