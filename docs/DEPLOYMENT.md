# Deployment

ABG Pulse v5 deployment source of truth: `main` branch of `NinjaRK/abg-pulse`.

The repository is connected to the Vercel project `abg-pulse-intelligence-v4`.

This update intentionally triggers the first Git-connected deployment after repository access and project linkage were completed on 3 September 2026.

Release gate: do not call the deployment successful until the public homepage, `/api/health`, and `/api/scan` are verified end to end.
