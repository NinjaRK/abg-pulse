{
  "name": "abg-pulse",
  "version": "6.0.0",
  "private": true,
  "type": "module",
  "description": "Dependable, comprehensive and auditable ABG intelligence PWA with dual evidence-weighted progress and governed snapshot ingestion.",
  "scripts": {
    "postinstall": "node bootstrap.mjs",
    "serve": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs && node tests/validate-data.mjs",
    "check": "node --check app.js && node --check core.mjs && node --check official.mjs && node --check api/scan.js && node --check api/events.js && node --check api/ingest.js && node --check api/social-ingest.js && node --check api/health.js && node --check api/progress.js && node tests/validate-data.mjs && node --check lib/live-snapshot.mjs && node --check scripts/refresh-live-snapshot.mjs",
    "refresh:snapshot": "node scripts/refresh-live-snapshot.mjs live-snapshot.json"
  },
  "engines": {
    "node": ">=20 <25"
  }
}
